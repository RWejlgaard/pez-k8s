apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - repository.yaml    # ArgoCD Repository (OCI helm registry) for the kro chart
  - application.yaml   # kro controller (via ArgoCD Application)
  - workload-rgd.yaml  # Workload CRD + reconcile graph
