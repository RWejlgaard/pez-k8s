apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../istio
  - ../../sealed-secrets
  - ingressgateway.yaml
  - shared-gateway.yaml
  - karpenter
# Monitoring disabled: k8s-monitoring.yaml and ../../grafana-cloud (namespace +
# credentials SealedSecret) are still on disk, just not wired in. Re-add both
# lines above to turn it back on.
