apiVersion: v1
kind: Service
metadata:
  name: pez-sh-canary
  namespace: pezsh
spec:
  ports:
  - name: pez-sh-canary
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: pez-sh
  type: ClusterIP
